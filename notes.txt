10 == "\n"
db == define bytes

Registers - are part of the processor that temporarily holds memory
x86_64 archtecture, registers hold 64 bits

System Call - or a syscall is when a program requests a service from the KERNEL

rdi == error code

x86_64 sections
.data section
.bss section
.text section


